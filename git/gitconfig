[github]
	user = ketan
	token = 47482877f97f18d0c4bb3a2573d3ecbb
[user]
	name = Ketan Padegaonkar
	email = KetanPadegaonkar@gmail.com
[alias]
	co = checkout
	st = status
	addremove = !"git add . && git ls-files --deleted | xargs --no-run-if-empty git rm"
	arm = !git addremove
	server = !"rackup $HOME/bin/git-serve/config.ru"
	browse = instaweb --httpd=webrick
[color]
	branch = auto
	diff = auto
	status = auto
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
[color "status"]
	added = yellow
	changed = green
	untracked = cyan
[merge]
	tool = opendiff
[daemon]
  receivepack = true
[receive]
	denycurrentbranch = ignore
